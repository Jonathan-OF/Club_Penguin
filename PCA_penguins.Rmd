---
title: "Penguins Database - PCA Analysis"
author: "Equipo 7  \nIntegrantes:  \nFajardo Zavala Yendali Viridiana  \nGonzález
  Rivas José Rafael  \nMaldonado Zamora Silvia Viridiana  \nOlivera Flores Jonathan
  \ \nValencia Quiroz Luis Roberto\n"
date: "`r Sys.Date()`"
output:
  html_document:
    df_print: paged
  pdf_document:
    latex_engine: xelatex
subtitle: Diplomado de métodos matemáticos y estadísticos para la Ciencia de Datos
---

# Descripción

El dataset "penguins.csv" es un conjunto de datos sobre las mediciones biométricas de pingüinos en las Islas Palmer, Antártida. Contiene 344 registros y 8 variables.

# Variables

1.  species: Categórica. La especie del pingüino (Adelie, Chinstrap o Gentoo).
2.  island: Categórica. La isla de observación (Torgersen, Biscoe o Dream).
3.  bill_length_mm: Numérica (float). Longitud del pico en milímetros. 
4.  bill_depth_mm: Numérica (float). Profundidad del pico en milímetros. 
5.  flipper_length_mm: Numérica (float). Longitud de la aleta en milímetros. 
6.  body_mass_g: Numérica (float). Masa corporal en gramos. 
7.  sex: Categórica. El sexo del pingüino (male o female).
8.  year: Numérica (integer). El año de la observación.


```{r setup, include = FALSE}

library(tidyverse)
library(tidymodels)
library(embed)
library(corrr)
library(tidytext)
library(gradethis)
library(sortable)
library(learntidymodels)
library(rstatix)
library(broom)
library(plotly)
library(plotly)
library(GGally)
library(tidyr)
library(FactoMineR)  
library(factoextra)
library(ggord)
library(knitr) 
library(kableExtra)
library(GGally)
library(FactoMineR)
library(factoextra)

```

```{r, include = FALSE}
# Carga de Datos
df<-read.csv("penguins.csv")

```

# Explorando el dataset 

```{r, echo = FALSE}

# Dimensión 

tibble(
  Métrica = c("Número de observaciones", "Número de variables"),
  Valor = c(nrow(df), ncol(df))
) %>%
  kable(caption = "Dimensión del conjunto de datos")

# HEAD

df %>%
  head() %>%
  kable(caption = "Primeras filas del conjunto de datos")

# Valores faltantes

df %>%
  summarise(across(everything(), ~ sum(is.na(.)))) %>%
  pivot_longer(everything()) %>%
  ggplot(aes(x = reorder(name, value), y = value)) +
  geom_col(fill = "purple") +
  geom_text(
    aes(label = value),
    hjust = -0.1,
    size = 4
  ) +
  coord_flip() +
  labs(
    title = "Número de registros faltantes por variable",
    x = "Variables",
    y = "Número de faltantes"
  ) +
  theme_minimal()

# Valores Duplicados

if (any(duplicated(df))) {
  cat("Se identificaron registros duplicados en el conjunto de datos.")
} else {
  cat("No se identificaron registros duplicados en el conjunto de datos.")
}

# Variable de clasificacion: diagnosis

df <- df %>% 
  mutate(species = as.factor(species))

```

```{r, include = FALSE}
# Número de especies 

df %>% count(species)

table(df$species)

```

```{r, echo = FALSE}

df %>%
  group_by(species) %>%
  summarise(n = n()) %>%
  mutate(prop = n / sum(n)) %>%
  ggplot(aes(x = species, y = n)) +
    geom_col(fill = c("#CC0033", "#e319dc", "#4A90E2")) +
    geom_text(aes(label = paste0(n, " | ",
                                 signif(n / nrow(df) * 100, digits = 4), '%')),
              nudge_y = 10) +
    ggtitle("Porcentaje de individuos por especie") +
    theme_gray()

# Nuevo dataframe

df1 <- df %>%
  select(where(is.numeric)) %>%  # solo variables numéricas
  select(-year) %>%              # excluimos el año 
  drop_na()                      # eliminamos los valores nulos 
```

#  Histogramas
```{r, echo = FALSE}

df1 |> 
  pivot_longer(
    everything(),
    names_to = "Variable",
    values_to = "Score"
  ) |>
  ggplot(aes(x = Score)) +
  geom_histogram(
    aes(y = after_stat(density)),
    bins = 20,
    colour = 3,
    fill = "darkmagenta"
  ) +
  facet_wrap("Variable", ncol = 4, scales = "free") +
  theme_minimal()


```

# Boxplots
```{r, echo = FALSE}
df1 |> pivot_longer(everything(),
                    names_to = "Variable",
                    values_to = "Score") |>
  ggplot(aes(y = Score)) +
  geom_boxplot(aes(fill = "darkred"),
               colour = 3, show.legend = FALSE) +
  facet_wrap("Variable", ncol = 4, scales = "free") +
  theme_minimal()

```

# Densidad
```{r, echo = FALSE}
df1 |> pivot_longer(everything(),
   names_to = "Variable",values_to="Score") |>
     ggplot(aes(x=Score)) + geom_density(aes(fill="darkred"),colour = 3,show.legend = FALSE) +
       facet_wrap("Variable",ncol = 4,scales = "free" ) + theme_minimal()

df1_con_clase <- df %>%
  select(species, where(is.numeric)) %>% 
  select(-year) %>% 
  drop_na()

```

# Comparación por la variable de clasificación: species

```{r, echo = FALSE}
df1_long <- df1_con_clase %>% 
  pivot_longer(!species, 
               names_to = "predictores", 
               values_to = "values")

theme_set(theme_light())

df1_long %>% 
  ggplot(aes(x = species, y = values, fill = predictores)) +
    geom_boxplot() + 
    facet_wrap(~ predictores, scales = "free", ncol = 4) +
    scale_color_viridis_d(option = "plasma", end = .7) +
    theme(legend.position = "none") +
    labs(title = "Comparación vía box-plot por especie")


df1_long %>% 
  ggplot(aes(values, fill = species)) +
    geom_histogram(color = "white", bins = 20) +
    facet_wrap(~ predictores, scales = "free", ncol = 4) +
    scale_color_viridis_d(option = "plasma", end = .7) +
    theme_light() +
    labs(title = "Comparación vía histograma por especie")

df1_long %>% 
  ggplot(aes(values, fill = species)) +
    geom_density(color = "white", alpha = 0.7) +
    facet_wrap(~ predictores, scales = "free", ncol = 4) +
    scale_color_viridis_d(option = "plasma", end = .7) +
    theme_light() +
    labs(title = "Comparación vía densidad por especie")

ggpairs(df1_con_clase, 
        mapping = aes(color = species),
        columns = 2:ncol(df1_con_clase))

```

# Correlación

```{r, include = FALSE}

cor_peng <- df1 %>% correlate(method = "spearman")
cor_peng

cor_matrix <- cor(df1, method = "spearman")
cor_matrix

```

```{r, echo = FALSE, warning = FALSE}

ggcorrplot::ggcorrplot(corr = cor_matrix,
                       type = "lower",
                       show.diag = TRUE,
                       lab = TRUE,
                       lab_size = 3)

kmo <- psych::KMO(cor_matrix)
                  
                  
```

```markdown
El índice de adecuación muestral de Kaiser–Meyer–Olkin (KMO) fue de 
**`r round(kmo$MSA, 2)`**, lo que indica una adecuación muestral 
adecuada para la aplicación del Análisis de Componentes Principales.

```

# Prueba de Bartlett
```{r, echo = FALSE}
bartlett <-  psych::cortest.bartlett(cor_matrix, n = nrow(df1))

```

```markdown
La prueba de esfericidad de Bartlett fue estadísticamente significativa 
(χ² = `r round(bartlett$chisq, 2)`, 
gl = `r bartlett$df`, 
p < 0.001), lo que indica que la matriz de correlación es adecuada 
para la aplicación del PCA.

```

```{r, include = FALSE}
cor.df1 <- df1 %>% cor_mat()
cor.df1

options(scipen = 999)

```


```markdown
Previo a la aplicación del Análisis de Componentes Principales (PCA), se 
evaluó la estructura de correlación de las variables. La matriz de 
correlación de Spearman mostró asociaciones estadísticamente 
significativas entre las variables (p < 0.001). La prueba de esfericidad 
de Bartlett fue significativa (χ² = 742.41, gl = 6, p < 0.001), indicando 
que la matriz de correlación difiere de una matriz identidad. Asimismo, 
el índice de adecuación muestral de Kaiser–Meyer–Olkin (KMO) presentó un 
valor de 0.70, lo que sugiere una adecuación muestral adecuada. En 
conjunto, estos resultados confirman que los datos son apropiados para 
la aplicación del Análisis de Componentes Principales.

```

```{r, echo = FALSE, warning = FALSE}

# Graficar matriz reordenada
cor.df1 %>%
  cor_reorder() %>%
  pull_lower_triangle() %>%
  cor_plot(label = TRUE)

```

```{r, include = FALSE, warning = FALSE}

#  Mostrar todos los pares de correlaciones
cor.df1 %>% cor_gather() %>% print(n = Inf)

```
```markdown
Se observaron correlaciones fuertes entre algunas variables, destacando 
la asociación positiva entre la longitud de la aleta y la masa corporal 
(r = 0.87, p < 0.001).
```

```{r, include = FALSE, warning = FALSE}
peng_recipe <- 
  recipe(~ ., data = df1_con_clase) %>% 
  update_role(species, new_role = "id") %>% 
  step_dummy(all_nominal_predictors()) %>%
  step_normalize(all_predictors()) %>% 
  step_pca(all_predictors(), id = "pca")
  # %>% step_pca(all_numeric(), threshold = 0.8)

peng_recipe

```

# PCA 

```{r, include = FALSE, warning = FALSE}

peng_pca <- prep(peng_recipe)

# Cargas de las variables en los componentes
pca_loading <- tidy(peng_pca, id = "pca")
pca_loading

# Varianza explicada por cada componente
pca_variances <- tidy(peng_pca, id = "pca", type = "variance")
pca_variances

# Porcentaje de varianza explicado por cada PC
pca_var_percent <- tidy(peng_pca, id = "pca", type = "variance") %>%
  filter(str_detect(terms, "percent variance"))
pca_var_percent

# Varianza acumulada
pca_var_cum_percent <- tidy(peng_pca, id = "pca", type = "variance") %>%
  filter(str_detect(terms, "cumulative percent variance"))
pca_var_cum_percent

```

```{r, echo = FALSE , warning = FALSE}

peng_pca %>% 
  tidy(id = "pca", type = "variance") %>% 
  dplyr::filter(terms == "percent variance") %>% 
  ggplot(aes(x = component, y = value)) + 
  geom_col(fill = "#B53389") + 
  xlim(c(0, 10)) + 
  geom_point(size=3) +
  geom_line(color="darkblue", size=1.1)+
  labs(x="PC", y="% de varianza", title="Scree plot")

peng_pca %>% 
  tidy(id = "pca", type = "variance") %>% 
  dplyr::filter(terms == "cumulative percent variance") %>% 
  ggplot(aes(x = component, y = value)) + 
  geom_col(fill = "#F25E52") + 
  xlim(c(0, 10)) + 
  geom_point(size=3) +
  geom_line(color="darkblue", size=1.1)+
  labs(x="PC", y="% acumulado de varianza", title="Scree plot")

```

```{r, include = FALSE , warning = FALSE}

variance_exp <- tidy(peng_pca,id = "pca", type = "variance") %>%
                filter(str_detect(terms, "percent variance"))

variance_exp

```


```{r, include = FALSE , warning = FALSE}

datos_pca <- bake(peng_pca, new_data = NULL)
datos_pca

```

# Grafica de los primeros dos componentes

```{r, include = FALSE , warning = FALSE}

VE <- paste(
  "Varianza explicada por dos componentes:",
  round(variance_exp$value[[1]] + variance_exp$value[[2]], digits = 2), "%"
)
VE

```

```{r, echo = FALSE , warning = FALSE}


datos_pca %>%
  ggplot(aes(x = PC1, y = PC2)) +
  geom_point() +
  labs(
    x = paste0("PC1: ", round(variance_exp$value[[1]], 2), "%"),
    y = paste0("PC2: ", round(variance_exp$value[[2]], 2), "%")
  ) +
  ggtitle(paste0("Penguins PCA: Gráfica de componentes principales", "\n", VE))


datos_pca %>%
  ggplot(aes(x = PC1, y = PC2, color = factor(species))) +
  geom_point() +
  labs(
    x = paste0("PC1: ", round(variance_exp$value[[1]], 2), "%"),
    y = paste0("PC2: ", round(variance_exp$value[[2]], 2), "%")
  ) +
  ggtitle(paste0("Penguins PCA: Gráfica de componentes principales", "\n", VE))

```

```{r, include = FALSE , warning = FALSE}
juice(peng_pca) %>%
  ggplot(aes(PC1, PC2, label = species)) +
  geom_point(aes(color = species), alpha = 0.9, size = 2) +
  geom_text(check_overlap = TRUE, hjust = "inward") +
  labs(color = NULL)

```

# Componentes

```{r, echo = FALSE , warning = FALSE}

peng_pca %>%
  tidy(id = "pca") %>%
  mutate(
    terms = tidytext::reorder_within(
      terms,
      abs(value),
      component
    )
  ) %>%
  ggplot(aes(abs(value), terms, fill = value > 0)) +
  geom_col() +
  facet_wrap(~component, scales = "free_y") +
  tidytext::scale_y_reordered() +
  scale_fill_manual(values = c("#b6dfe2", "#0A537D")) +
  labs(
    x = "Absolute value of contribution",
    y = NULL,
    fill = "¿Positive?"
  )


peng_pca %>%
  tidy(id = "pca") %>%
  filter(component %in% paste0("PC", 1:3)) %>%
  mutate(component = fct_inorder(component)) %>%
  ggplot(aes(value, terms, fill = terms)) +
  geom_col(show.legend = FALSE) +
  facet_wrap(~component, nrow = 1) +
  labs(y = NULL)

```

# 3D
```{r, echo = FALSE , warning = FALSE}

datos_pca$species <- as.factor(datos_pca$species)


g.df1 <- plot_ly(
  datos_pca,
  x = ~PC1,
  y = ~PC2,
  z = ~PC3,
  color = ~species,
  colors = c("#0000FF", "#FF00FF", "#00AA00")  
) %>%
  add_markers() %>%
  layout(
    scene = list(
      xaxis = list(title = "PC1"),
      yaxis = list(title = "PC2"),
      zaxis = list(title = "PC3")
    )
  )

g.df1



fig <- plot_ly(
  datos_pca,
  x = ~PC1,
  y = ~PC2,
  z = ~PC3,
  color = ~species,
  colors = c('#BF382A', '#0C4B8E', '#E3AA00')  
) %>%
  add_markers(size = 4) %>%
  layout(
    scene = list(
      xaxis = list(title = "PC1"),
      yaxis = list(title = "PC2"),
      zaxis = list(title = "PC3"),
      camera = list(eye = list(x = 1.1, y = 1.1, z = 0.8))
    ),
    title = "PCA 3D — Penguin Species"
  )

fig

```

# EXTRAS

```{r, echo = FALSE , warning = FALSE, message = FALSE}


df1_con_clase$species <- as.factor(df1_con_clase$species)
datos_pca$species     <- as.factor(datos_pca$species)


ggpairs(
  df1_con_clase,
  mapping = aes(color = species),
  columns = 2:ncol(df1_con_clase)
)

ggpairs(
  datos_pca,
  mapping = aes(color = species),
  columns = 1:3   
)


res.pca <- PCA(df1_con_clase[, -1], scale.unit = TRUE)

fviz_pca_var(
  res.pca,
  alpha.var = "contrib",
  col.var = "contrib",
  gradient.cols = c("#00AFBB", "#E7B800", "#FC4E07"),
  title = "Influencia de las variables en PCA1 y PCA2",
  repel = TRUE
)

fviz_pca_ind(
  res.pca,
  col.ind = df1_con_clase$species,
  palette = c("#00AFBB", "#E7B800", "#FC4E07"),
  title = "Distribución de los individuos en PCA1 y PCA2",
  repel = TRUE
)


fviz_pca_biplot(
  res.pca,
  repel = TRUE,
  title = "Biplot",
  col.var = "#2E9FDF",
  col.ind = df1_con_clase$species,
  palette = c("#00AFBB", "#E7B800", "#FC4E07")
)


fviz_pca_biplot(
  res.pca,
  repel = TRUE,
  col.var = "blue",
  col.ind = df1_con_clase$species,
  palette = c("#00AFBB", "#E7B800", "#FC4E07"),
  addEllipses = TRUE,
  ellipse.level = 0.95,
  title = "Biplot con elipses por especie"
)
```
```markdown

El Análisis de Componentes Principales permitió reducir la dimensionalidad
del conjunto de datos sin pérdida significativa de información. Los dos
primeros componentes principales explicaron el 88.2 % de la varianza total,
lo que evidencia que la estructura de los datos puede describirse
adecuadamente en un espacio bidimensional.

El primer componente principal estuvo asociado principalmente con el
tamaño corporal de los pingüinos, integrando variables como la longitud
de la aleta, la masa corporal y la longitud del pico. El segundo componente
capturó variaciones relacionadas con la morfología del pico, particularmente
su profundidad y longitud.

La proyección de los individuos en el espacio definido por los dos primeros
componentes mostró una clara diferenciación entre las especies, destacando
la separación de la especie Gentoo respecto a Adelie y Chinstrap. Estos
resultados concuerdan con las diferencias morfológicas conocidas entre las
especies, lo que respalda la validez del análisis realizado.

En conjunto, los resultados indican que el PCA es una herramienta adecuada
para explorar y resumir la variabilidad morfológica de los pingüinos, así
como para identificar patrones asociados a la clasificación por especie.
```




